{
  "name": "ectojs",
  "version": "1.0.0",
  "description": "Ecto JS",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "lint": "tslint --config tslint.json --project . '**/*.ts'",
    "format": "prettier --write 'src/**/*.ts'",
    "check-formatted": "prettier --check 'src/**/*.ts'",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "jest": "^24.8.0",
    "prettier": "^1.18.2",
    "ts-config": "^20.9.0",
    "ts-jest": "^24.0.2",
    "tslint": "^5.18.0",
    "typescript": "^3.5.2"
  }
}